namespace Vent.Core.Items
{
    /// <summary>Things left lying around the office that change what the player can see. Found by looking and pressing Interact, like the patch cables.</summary>
    public enum OfficeItem
    {
        /// <summary>A floor plan of the building: a translucent overlay on the HUD, toggled with the map key.</summary>
        BuildingMap,
        /// <summary>A vanity mirror: a persistent rear view at the top of the HUD.</summary>
        RearViewMirror,
    }
}
